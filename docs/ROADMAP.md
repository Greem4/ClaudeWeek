# Куда дальше

Честный список пробелов на 2026-08-07 — после аудита. Не «идеи на будущее», а
то, чего в приложении сегодня нет, с ценой вопроса и тем, что мешает сделать.

Порядок — по тому, насколько заметно отсутствие в ежедневной работе.
Что уже исправлено аудитом, здесь не повторяется: см. [журнал в конце](#что-закрыл-аудит-2026-08-05).

---

## Оглавление

- [Соседи по полке](#соседи-по-полке) — что умеют похожие программы
- [Заметно сразу](#заметно-сразу) — 1–2
- [Заметно в углах](#заметно-в-углах) — 3–8
- [Ограничения, с которыми живём](#ограничения-с-которыми-живём) — 9–10
- [Что сознательно не делаем](#что-сознательно-не-делаем)
- [Что закрыл аудит](#что-закрыл-аудит-2026-08-05) — журналы 2026-08-05, 06 и 07
- [Что сделано после аудита](#что-сделано-после-аудита) — журналы 2026-08-08, 10, 15, 16, 17, 31 и 2026-09-01

---

## Соседи по полке

Смотрел, что умеют другие счётчики лимита Claude Code, — не чтобы копировать
всё подряд, а чтобы понять, чего людям не хватает настолько, что они это
написали.

| Программа | Чем берёт | Что стоит подсмотреть |
|---|---|---|
| [CCSeva](https://github.com/Iamshankhadeep/ccseva) | нативная Swift, вкладки «дашборд / живой / аналитика», графики Swift Charts, инкрементальный обход `~/.claude/projects` | уведомления на 70 % и 90 % с остыванием и только на ухудшении; разбивка по моделям и проектам |
| [ClaudeUsageBar](https://www.claudeusagebar.com/) | сессия, неделя, недельный лимит Fable, докупленные кредиты в долларах, статус сервисов Claude | пороги уведомлений 25/50/75/90; глобальная горячая клавиша (⌘U); отдельная строка на каждый лимит |
| [cc-usage-bar](https://github.com/lionhylra/cc-usage-bar) | запускает `claude` и снимает вывод `/usage` — «всегда точно, нулевая настройка» | ничего: путь дорогой (процесс на каждое обновление), у нас тот же ответ берётся напрямую |
| [cctray](https://github.com/goniszewski/cctray), [claude-usage-tracker](https://github.com/penicillin0/claude-usage-tracker-for-mac) | обёртки над CLI `ccusage` | ничего: тянут за собой Node и чужой формат |
| [claude-monitor](https://github.com/rjwalters/claude-monitor) | виджет строки меню поверх локальных данных | ничего нового |

**Чего нет ни у кого из них, а у нас есть:** плановая зона на полосе. Все
остальные показывают «сколько осталось», и ни один — «иду я в графике или
обгоняю». Ещё: календарное недельное окно с переводом часов, честная пометка
оценки (`≈`), самокалибровка офлайна по официальному проценту и палитра,
прогнанная через валидатор на дальтонизм. Ни Node, ни `ccusage`, ни отдельной
авторизации.

**Чего нет у нас, а есть у них** — пункты 1, 2, 4, 5 ниже. Уведомления, самое
частое у соседей, у нас теперь свои — с двумя порогами на каждый лимит.

---

## Заметно сразу

### 1. Пятичасовая сессия важнее недели, а истории у неё нет

В сессию упираются чаще, чем в неделю, но у неё нет ни истории, ни прогноза —
только полоса, обратный отсчёт и уведомление о пороге. Офлайн она исчезает
совсем (см. п. 4).

*Что делать:* подпись «в прошлый раз хватило до 18:40» из истории окон.

*Цена:* половина работы п. 2.

### 2. История не копится

Прошлые недели нигде не хранятся: `cache.json` держит только текущее окно.
Сравнить «эта неделя против прошлой», увидеть тренд или средний темп нельзя.
У CCSeva под это отдельная вкладка с графиками.

*Что делать:* дописывать в `history.json` итог недели в момент сброса
(границу видно по `WeekWindow`), хранить 8–12 записей, показывать спарклайн в
футере панели и среднее «обычно к четвергу у вас 60 %».

*Цена:* день с UI. *Мешает:* ничего.

---

## Заметно в углах

### 3. Лимиты по моделям и докупленные кредиты не показываются

В ответе сервера есть `seven_day_opus`, `seven_day_sonnet`,
`seven_day_oauth_apps`, `extra_usage` и `spend`. Мы читаем только общий
недельный и пятичасовой. На тарифе Max первые приходили `null`, поэтому их и
не разбирали, — но у других тарифов там числа, и соседи их показывают
(ClaudeUsageBar рисует отдельную строку под недельный лимит Fable и
докупленные кредиты в долларах).

*Что делать:* разобрать поля в `UsageResponse`, показывать строку под каждый
непустой лимит — в той же сетке, что сессия. Настройка «показывать лимиты по
моделям», по умолчанию включена, если сервер их прислал.

*Цена:* полдня. *Мешает:* нужен живой ответ с непустыми полями — проверить
можно только на подходящем тарифе (`swift scripts/probe-usage.swift`).

### 4. Пятичасовая сессия офлайн не считается

Локальный источник её не вычисляет: веса моделей приближают недельную формулу,
а не сессионную. Офлайн показывается последнее официальное значение и только
пока не истекло его окно — потом строка исчезает.

*Что делать:* либо честно оставить как есть (текущее решение), либо
калибровать вторую формулу по официальным ответам так же, как недельную.
Второе — исследование на несколько дней с риском выдумать точность, которой
нет.

### 5. Разбивки по проектам нет

Транскрипты лежат по каталогам проектов, стоимость каждого уже считается —
но наружу выходит только сумма. «На что ушла неделя» ответить нечем.

*Что делать:* группировать `UsageRecord` по каталогу проекта (он есть в пути),
добавить в `--json` и отдельным разделом в панель или в новое окно истории.
Осторожно с приватностью: имена каталогов — это имена рабочих проектов, в лог
и в кеш они попадать не должны.

*Цена:* день вместе с UI.

### 6. VoiceOver не прослушан вживую

Разметка есть: у каждой строки свой ярлык, у панели — сгруппированный
заголовок. Ушами это никто не проверял, и порядок обхода может оказаться
неудобным.

*Что делать:* включить VoiceOver, пройти панель и настройки, поправить
`accessibilityLabel` и порядок. Полчаса работы, которую нельзя
автоматизировать.

### 7. Тесты не покрывают UI и настройки

XCTest без Xcode недоступен, поэтому проверки написаны своим раннером и
охватывают ядро: окно недели, план, форматирование, оба провайдера, конфиг,
свежесть данных. Всё, что рисует, проверяется глазами через `--screenshot`.

Отдельно не покрыта логика окна настроек: применение на лету, дебаунс записи,
пересоздание провайдера и перезапуск таймеров при смене полей. Ошибка здесь
тихая — настройка «применилась», но до провайдера не дошла (именно так и
пропал интервал обновления, см. журнал аудита).

*Что делать:* вынести решение «что пересоздавать» из `applyFromSettings` в
чистый тип без AppKit и проверить таблицей «поле → что перезапускается».
Снапшот-тесты панели через `ImageRenderer` с эталонными PNG — вторым заходом.

*Цена:* день на оба. *Мешает:* эталоны придётся обновлять при каждой правке
вёрстки.

### 8. Мелочи, замеченные аудитом

- **Двойной обход транскриптов.** За один цикл обновления `LocalProvider.scan`
  зовётся дважды с теми же аргументами: за формой недели и за калибровкой.
  Кешировать итог нельзя (обход обязан отражать диск — попытка описана в
  комментарии к `scan`), а вот провести стоимость через снимок и не сканировать
  второй раз — можно. Экономия — один инкрементальный проход в 0.04 с.
- **Глобальная горячая клавиша.** У ClaudeUsageBar — ⌘U из любого места.
  Требует разрешения «Универсального доступа», поэтому только по явной просьбе
  и с объяснением, зачем.
- **Новость об обновлении никуда не ведёт.** Кнопка «Обновить» живёт на
  вкладке «О программе» (`AboutSettings`), а дойти до неё человек обязан сам:
  строка «↑ вышла версия …» в панели зовёт `onSettings` и открывает окно на
  первой вкладке, «Общие»; баннер `announceUpdate` пересказывает дорогу
  словами, а нажатие на него не обрабатывается вовсе — у `BannerDelegate` есть
  только `willPresent`. Надо, чтобы и строка, и баннер приводили ровно к
  кнопке обновления. Мешает: `TabView` в `SettingsView` без `selection` —
  вкладку не выбрать программно; нужны состояние выбранной вкладки в
  `SettingsModel` и `didReceive` в делегате центра уведомлений.
- **Процент в панели молчит, пока полоса уже горит.** На снимке сессии полоса
  жёлтая на 96 %, а цифра «96 %» справа — обычная белая: она красится только
  при `session.isExhausted`, то есть от 100 % (`SessionRow.swift`, строки 112
  и 134 — `isExhausted ? critical : primaryText`). Порог у полосы другой и уже
  посчитан рядом: `isWarning` = `state != .normal`, то есть пороги лимита из
  настроек (81/95). Надо, чтобы цифра краснела по тому же порогу, что красит
  полосу и дугу кольца, — тем более что красный в саму заливку не идёт
  намеренно: с зелёным он неразличим при дейтеранопии, и тревога по замыслу
  живёт как раз в тексте и значке (комментарий к `SessionBar`). Сейчас текст
  до сотни не тревожится вовсе. Речь про панель; кольцо строки меню, где дуга
  и цифра показывают разные лимиты и красятся раздельно, не трогаем.

---

## Ограничения, с которыми живём

### 9. Эндпоинт не документирован публично

`/api/oauth/usage` снят с живого ответа и с клиента. Схема может измениться
без предупреждения. Декодер написан так, чтобы пропажа поля давала честную
ошибку, а не молчаливый ноль, и приложение уходило на локальную оценку — но в
этот момент точность падает до калибровки.

*Страховка:* `swift scripts/probe-usage.swift` печатает живой ответ целиком;
запасной путь через `limits[]` уже есть.

### 10. Сборки не нотаризованы

Лицензия (MIT) и CI появились, релиз собирается по тегу и выкладывается
`.dmg` под Apple Silicon; Intel-образа в релизах нет вовсе — там сборка из
исходников. Чего в этой цепочке нет — подписи Developer ID и
нотаризации: образ подписан ad-hoc, и первый запуск скачанного приложения
упирается в Gatekeeper, пока не снимешь карантин руками.

Постоянная подпись при этом уже есть, но другая и для другого:
`scripts/signing-cert.sh` заводит самоподписанный сертификат, чтобы designated
requirement не менялся и приложение держалось в списке доверенных у записи с
токеном ([USAGE](USAGE.md#чтобы-разрешение-спросили-один-раз)). Второй
проверке, partition list, он не помогает — там нужен `teamid:` от Apple, и
обходится это чтением через `/usr/bin/security`
([USAGE](USAGE.md#почему-одного-сертификата-не-хватило)). Gatekeeper подпись не
касается вовсе: тот верит только Developer ID с нотаризацией.

*Что делать:* ничего, пока нет Apple Developer Program — из РФ её штатно не
оплатить (та же причина, что у виджета Центра уведомлений). Появится
сертификат — `codesign --sign "Developer ID Application"` и `notarytool submit
--wait` встают в `make-dmg.sh` и в `release.yml` двумя шагами.

---

## Что сознательно не делаем

- **Виджет Центра уведомлений** — требует App Groups, а та доступна только по
  платной подписке Apple Developer Program, которую из РФ штатно не оплатить.
  Разбор — в приложении Б [плана](PLAN.md).
- **Обновление OAuth-токена.** Refresh-цикл — дело Claude Code; два процесса,
  наперегонки меняющие одну запись Keychain, теряют токен. На `401` мы уходим
  на локальную оценку и ждём, пока Claude Code обновит токен сам.
- **Красная заливка полосы.** Красный и зелёный неразличимы при дейтеранопии,
  поэтому красный живёт только в заголовке и иконке, рядом со словом и знаком.
- **Обёртка над `ccusage` или запуск `claude` ради `/usage`.** Соседи так
  делают; это тянет за собой Node или процесс на каждое обновление ради того
  же ответа, который мы берём одним GET.
- **Кеширование обхода транскриптов.** Пробовали (см. комментарий к
  `LocalProvider.scan`): за один цикл экономит один инкрементальный проход, а
  взамен теряет дописанные строки. Плохой обмен.

---

## Что закрыл аудит 2026-08-05

Чтобы не искать это снова:

| Было | Стало |
|---|---|
| официальное число во время паузы после отказа отдавалось как свежее — бесконечно | годится 15 минут, потом `auto` уходит на локальную оценку |
| снимок помечался моментом запроса, а не моментом ответа | `fetchedAt` — момент наблюдения; старше двух минут кружок источника желтеет и говорит «данные offline» |
| «Обновлять раз в» из настроек не применялось до перезапуска | таймер перезапускается на смене интервала |
| автокалибровка на 1–2 % давала бюджет с ошибкой вдвое | калибруем от 5 % и выше, ниже — держим прежний бюджет |
| «Бюджет недели» в «О программе» всегда «не подобран» | читается подобранный из кеша, с пометкой, откуда взялся |
| подпись порога в настройках описывала не то, что делает код | подписи исправлены, добавлено пояснение про суточные полосы |
| `--jsonn`, `--provider=bogus`, `--calibrate=abc` молча поднимали строку меню | ошибка с подсказкой и кодом 2 |
| «Сбросить настройки» срабатывало без вопроса | подтверждение |
| версия жила в двух местах и могла разъехаться | `make-app.sh` берёт её из `Version.swift` |
| мёртвый код в `Theme` и `DropdownPanel`, 18 предупреждений в сборке тестов | убрано, сборка чистая |

## Что закрыл аудит 2026-08-06

| Было | Стало |
|---|---|
| панель на части рабочих столов не открывалась с первого клика — весь механизм `DropdownPanel` спрашивал `isOnActiveSpace` у окна с `.canJoinAllSpaces`, для которого этот вопрос смысла не имеет (см. «Грабли» в [ARCHITECTURE.md](ARCHITECTURE.md)) | «показана» спрашивается у WindowServer (`CGWindowListCopyWindowInfo`, `isOnActiveSpaceScreen`), а не у окна; на смене стола закрывается та панель, которой на активном столе нет, — закрытие без разбора гасило открытую сразу после свайпа |
| закреплённая на время настроек панель переживала отключение или смену монитора без пересчёта места | подписка на `didChangeScreenParametersNotification` пересчитывает позицию |
| дебаунс записи `config.json` (400 мс) терял последнюю правку настройки, если выйти из приложения внутри этого окна | `applicationWillTerminate` дописывает отложенную правку синхронно |
| `ModelPricing.reportedUnknown` — общий `Set` без блокировки, мутировался из нескольких параллельных экземпляров `LocalProvider` (гонка данных) | доступ обёрнут в `NSLock` |
| счётчик проверок в документации (`README.md`, `ARCHITECTURE.md`) отставал от факта — 247 вместо 299 | приведён к актуальному числу |
| рабочий день в `WorkHours.seconds` строился прибавлением часов к полуночи, и в сутки перевода часов съезжал: осенью план начинал расти в 10:00, весной в 12:00 | границы дня ищутся по циферблату (`date(bySettingHour:)`), сутки перевода весят столько же, сколько обычные; покрыто проверками в обе стороны |
| `Formatting.age` («3 мин назад») никем не вызывался с тех пор, как панель перешла на «данные online / offline», но жил и тестировался | убран вместе с проверками |
| `--screenshot --verbose` и `--icon --verbose` заводили каталог с именем «--verbose»: следующий аргумент брался за путь, каким бы он ни был | аргумент, начинающийся с «-», считается соседним флагом (`CLI.directory(after:in:)`) |
| `--screenshot` рисовал картинки для README личными настройками автора: свёрнутая панель, чужая палитра и пороги выглядели бы как поведение программы | вид и пороги на снимках заводские, окно недели и рабочий день — из конфига |
| окно настроек создавалось один раз за запуск и правки `config.json`, сделанные руками, не подхватывало: второй показ выкладывал старые значения, а первый же ползунок затирал ими файл | конфиг принимается окном при каждом показе и при перечитывании файла (`SettingsModel.adopt`) |
| комментарии и документация обещали то, чего в панели нет: возраст данных в подсказке кружка, «темп 1.2×» в футере, план на середину суток, несохраняемое округление `resets_at` | приведены к тому, что делает код |
| `PLAN.md` отстал от кода сильнее прочих: план по рабочим часам не описан вовсе, `resetHour` в примере конфига остался 15, дерево без `WorkHours`, `SourceDot` и `MenuBarRing`, счётчик проверок 212 | разделы 2.2, 2.4, 4.1, 5.1, 5.5, 6 и 8 приведены к факту |
| репозиторий нельзя было показать: ни лицензии, ни CI, ни готовой сборки — только «склонируйте и соберите» | MIT, `ci.yml` на каждый push, `release.yml` по тегу с универсальным `.dmg` и контрольной суммой |
| README на 549 строк был документацией, а не витриной: установка тонула между таблицами настроек и разбором расчёта плана | README — страница проекта, подробности переехали в [USAGE.md](USAGE.md) целиком |

## Что закрыл аудит 2026-08-07

| Было | Стало |
|---|---|
| «сборка без предупреждений» обещал README, но CI собирал обычным `swift build` — предупреждение прошло бы незамеченным | CI собирает с `-warnings-as-errors`: обещание проверяется, а не даётся на словах |
| `uninstall.sh` снимал агента только по нынешнему label и не трогал копию в `/Applications`: после установки из образа приложение оставалось и поднималось снова | снос тот же, что у `install.sh`, — все агенты, чьи plist поминают ClaudeWeek, и обе копии приложения |
| комментарий в `uninstall.sh` и §4.4 плана уверяли, что скрипт зовётся из `install.sh --quiet`; тот давно сносит прежнюю версию сам | описание приведено к тому, что делают скрипты |
| `PanelMaterial.hud` — вариант материала, которого не брала ни одна палитра | убран |
| `--calibrate=0` отвергался сообщением «процент должен быть в диапазоне 0…100», то есть ссылался на диапазон, в который сам входит | «больше 0 и не больше 100» |
| счётчик проверок отставал от факта — 331 вместо 336 (`README.md`, `ARCHITECTURE.md`, `PLAN.md`) | приведён к актуальному числу |
| `ARCHITECTURE.md` и `PLAN.md` считали вкладок настроек четыре, `USAGE.md` звал вкладку «Панель» прежним именем «Внешний вид» | названия и число вкладок совпадают с тем, что в окне |
| дерево проекта в `PLAN.md` осталось без `LoginItem.swift`, `make-dmg.sh` и `probe-usage.swift` | дерево дополнено |
| ROADMAP считал автозапуск нерешённым и предлагал `SMAppService` — тот самый механизм, который `LoginItem` заводить запрещает | пункт переписан: галочка есть, не хватает её дубля в окне настроек |
| журнал аудита 2026-08-06 описывал промежуточное решение про смену рабочего стола, а не то, чем оно кончилось | запись приведена к нынешнему коду |
| панель гасла сама через секунду после появления и не закрывалась щелчком по значку: `isShown` держался на списке окон (`CGWindowListCopyWindowInfo`), а тот отстаёт и не знает окна, показанного мгновение назад — в логе `наЭкране=false` стоит в строке сразу после `orderFrontRegardless` | панель живёт на одном столе (`.moveToActiveSpace`) и отвечает про себя сама (`panel.isOnActiveSpace`); разбор, измерения и пробы — [SPACES.md](SPACES.md), `scripts/probe-panel.swift` |

## Что сделано после аудита

Здесь не разбор ошибок, а работа, закрывшая пункты списка выше.

### 2026-08-08

| Было | Стало |
|---|---|
| автозапуск жил галочкой в меню по правому клику — там, где настройки не ищут (бывший пункт 4) | строка «Запускать при входе в систему» первой секцией вкладки «Общие»; из меню пункт убран, чтобы одно и то же состояние не показывалось в двух местах. Механизм прежний — плист `LoginItem`, `SMAppService` по-прежнему не заводим |
| автообновления не было: о новой версии узнавали случайно, ставили её тем же `install.sh` (бывший пункт 11) | программа спрашивает GitHub при запуске, раз в сутки и после сна; найденный выпуск показывает строкой в панели, а ставится он одной кнопкой на вкладке «О программе» — с обычными окнами macOS: заметки к релизу перед установкой, вопрос про перезапуск после. Кнопка качает образ релиза, сверяет SHA256 с `SHA256SUMS.txt`, проверяет подпись и версию внутри и заменяет бандл одним движением (`Updater`, `UpdateInstaller`, `UpdateController`, `--update`). Из меню по правому щелчку обновление убрано по той же причине, что и автозапуск: одно состояние — одно место. Sparkle не заводили: appcast и Developer ID для проекта без сертификата — лишняя площадка, а сумма из релиза даёт ту же проверку целостности |
| синяя рамка фокуса вокруг ⚙ и ⟳ в футере панели: AppKit обводит нажатую кнопку прямоугольником, и на панели размером с меню он читается как ошибка вёрстки | `.focusEffectDisabled()` на обеих кнопках — то же решение, что уже стояло на кружке источника |

### 2026-08-10

| Было | Стало |
|---|---|
| релиз собирался на раннере `macos-15`, то есть с SDK 15, и на macOS 26 система рисовала такому бандлу прежнее плоское оформление: скачанная 0.1.5 выглядела старее сборки, собранной дома на CLT 26 | оба workflow идут на `macos-26`, а после сборки бандла сверяют SDK через `vtool` и падают, если раннер принёс что-то старее 26. Минимальная версия системы к SDK не привязана — она задана в `Package.swift` (`.macOS(.v14)`), поэтому образ по-прежнему запускается на macOS 14 и 15 |
| CI падал на `Keychain.swift`: `kSecUseAuthenticationUIFail` устарел с macOS 11, а `-warnings-as-errors` превращает предупреждение в ошибку. Замены у ключа нет — LAContext диалог не гасит, и это проверено | группа `DeprecatedDeclaration` понижена обратно до предупреждения флагом `-Wwarning`: оно остаётся в логе на виду, но сборку не роняет. Понижение общее на пакет, так что новое устаревшее место придётся замечать глазами |
| заводские настройки расходились с тем видом, к которому пришла обкатка: полоса вместо кольца, системная палитра, недельный ряд, круглые пороги 80/95 | дефолты — кольцо, `contrast`, компактная панель, вуаль 0.6, скругление 16, подпись сброса «и то, и другое», прогноз выключен, пороги 81/93 у недели и 81/95 у сессии. Картинки README снимаются заводским видом, и только недельный ряд для них задаётся явно: витрина показывает то, ради чего программа написана |

### 2026-08-15

| Было | Стало |
|---|---|
| уведомлений не было вовсе: о приближении к лимиту сообщал только цвет значка, и если панель не открывать, момент «осталось 10 %» проходил мимо (бывший пункт 1) | баннер macOS по своим порогам — два на недельный лимит (80 и 95 %) и два на пятичасовую сессию (75 и 95 %), вкладка «Уведомления» в настройках. В баннере лимит, живой процент, остаток и час сброса, рядом то же кольцо, что в строке меню, и того же цвета. Три правила из CCSeva на месте: об одном пороге один раз за окно лимита, только на ухудшении, остывание пять минут. Сказанное лежит в `alerts.json` и переживает перезапуск. Правила целиком в ядре (`AlertPlanner`) и покрыты тестами — показанное дважды уведомление глазами не ловится |
| по плану уведомления должны были выключаться по умолчанию, чтобы виджет не стал источником шума | включены сразу: молчащая по умолчанию функция, ради которой её и добавляли, требует сначала найти вкладку. Шум держат не тумблером, а правилами выше — за неделю баннеров выходит четыре, по два на лимит. Выключается всё одним тумблером или по лимиту в отдельности |

### 2026-08-16

| Было | Стало |
|---|---|
| интерфейс был только русский: тексты вшиты в код, а все соседи по полке англоязычны — это и мешало показать проект за пределами своего стола (бывший пункт 11) | английский интерфейс в 0.1.11. Язык следует за системным, а на вкладке «Общие» выбирается явно: «Как в системе», «Русский», «English»; переключается на ходу — язык лежит в конфиге, и панель с настройками перерисовываются по нему тем же движением, что и на любую другую правку. Переведено всё, что видит человек, вместе со склонениями дней, длительностей и единиц токенов; лог остаётся русским — одну ошибку не должно быть видно двумя разными строками. На один запуск язык задаётся ключом `--lang=system\|ru\|en`, им же снимаются английские картинки. Строки лежат таблицей в коде (`Localization.swift`, `L10n`) и проверяются тестом на полноту: `.strings` потребовали бы чинить ещё и сборку — `make-app.sh` кладёт в бандл один бинарь и ресурсы SwiftPM не переносит. План, по которому это делалось, — [L10N.md](L10N.md) |

### 2026-08-17

| Было | Стало |
|---|---|
| на двух мониторах, поставленных один над другим, панель вставала не под строкой меню, а у нижнего края соседнего экрана — оторванная от значка на пол-экрана. Строка меню в полноэкранном режиме прячется, и её окно вместе со значком уезжает вверх на свою высоту: у вплотную стоящих мониторов этих 30 точек хватает, чтобы значок оказался на территории соседа, а `anchor.screen` честно ответил «соседний». Дальше панель клэмпилась по чужим границам — замер совпал с расчётом до точки: значок `y1080…1110` → панель `y1088…1416`, где `1088 = 1080 + 8` | экран выбирает `DropdownPanel.screen(for:)` по признаку, который сдвиг строки меню переживает: она всегда прижата к верхней кромке своего экрана, значит значок принадлежит тому из накрывающих его по горизонтали мониторов, чья верхняя кромка ближе. Верхняя кромка панели вдобавок ограничена `screen.maxY` — спрятанная строка меню уводила бы за край и её. Разбор и замеры — [SPACES.md](SPACES.md), §7; проба `probe-panel.swift --displays` |

### 2026-08-31

| Было | Стало |
|---|---|
| виджет показывал ровно один аккаунт — тот, под которым сейчас работает Claude Code, — и поставить рядом рабочий с личным было нельзя. Прежние цифры при переключении не сохранялись, а отбрасывались (бывший пункт 10) | два аккаунта в панели. Claude Code различает их конфиг-домом, и второй дом (`~/.claude-b` плюс `CLAUDE_CONFIG_DIR`) держит свой токен, не выбивая первый. Появился каталог — в заголовке встают два знака Claude; выбранный горит оранжевым, второй гаснет до серого, а кто из них кто, говорит подсказка с живым адресом из `claude auth status`. Состояние хранится по аккаунту, а не одним файлом: у второго свои `secondary-cache.json`, `secondary-alerts.json`, `secondary-state.json` и `secondary-index.json`, поэтому обратное переключение мгновенно и ничего не теряет. Имя записи Keychain второго дома Claude Code не публикует, но собирает его из пути дома (`Claude Code-credentials-<первые 8 hex sha256 пути>`) — ClaudeWeek повторяет ту же формулу, и сторожит её тест с живым вектором. Искать запись по `organizationUuid` внутри неё, как было задумано сперва, больше нельзя: 2.1.251 убрал это поле из записи, и принадлежность токена теперь называет только `claude auth status`. Каталога нет — нет и переключателя. Осталось за бортом: аккаунтов ровно два, произвольного числа нет — под него пришлось бы заводить список в конфиге, а спроса на третий пока не видно |
| приложение из релиза 0.2.1 падало через секунду после запуска, и обновление выглядело как «клад падает и не обновляется»: образ вставал успешно, но новая версия умирала, а в строке меню оставалась прежняя — и назавтра всё повторялось | знак Claude читается через `Bundle.main` из `Contents/Resources`, а не через `Bundle.module`: генерируемый SwiftPM аксессор ищет ресурсный бандл в корне `.app` и по абсолютному пути в `.build` машины сборки, поэтому у собиравшего работал, а у всех остальных — `fatalError`. `make-app.sh` теперь проверяет, что каждый ресурс лёг туда, откуда его возьмёт `Bundle.main`, и не собирает бандл, если `Bundle.module` вернётся в код приложения: прежняя проверка убеждалась лишь в том, что ресурсный бандл есть в `.build`, — он был на месте всё то время, пока приложение падало. Пропавшая картинка теперь заменяется вопросительным знаком, как и задумано, а не роняет процесс |
| `claude auth status` спрашивали всегда с `CLAUDE_CONFIG_DIR`, и вкладка «Доступ» показывала «не вошли» у аккаунта, в котором человек работает прямо сейчас | переменная ставится только для нестандартного дома. Выставленная даже в тот же самый путь `~/.claude`, она заставляет Claude Code отвечать `loggedIn: false` — ячейку учётных данных выбирает не путь, а факт установки переменной (2.1.251). Унаследованная из окружения снимается: приложение могли запустить из оболочки под вторым аккаунтом. Грабли записаны в [ARCHITECTURE.md](ARCHITECTURE.md#грабли) |

### 2026-09-01

| Было | Стало |
|---|---|
| нижние две строки ряда приходили к 100 % обеими сразу, а расход последних часов недели пропадал вместе со строкой, которой им не досталось | при сбросе среди ночи последние сутки окна — огрызок в пару часов вне рабочих часов: плана он не набирает нисколько и повторял сотню предыдущих суток. Такой огрызок своей строки больше не получает (`WeekWindow.rowSlots`), день сброса остаётся в ряду первой половиной, а расход, случившийся в нём, доливается в последнюю строку (`UsageSnapshot.rows(at:)`) — иначе ряд заканчивался бы числом меньше итога в футере. Там, где у последних суток рабочие часы есть, всё как было |
| после сброса счёта суточные полосы пустели целиком: `— / 43 %` во всех сутках при непустой неделе, а футер показывал свои проценты | отсечка убирает прежний расход из локального счёта, но не из недельного итога — его считает сервер, который об отсечке не знает. Теперь до-отсечный расход помнится отдельно (`LocalUsage.costBeforeCount`) и раскладывается по дням тем же ровным темпом, каким считается план, — от начала окна до момента сброса (`carriedShares`). Ряд идёт вдоль плановой линии и приходит к общему проценту в сутках сброса; горкой в один день этот расход не сваливается намеренно — в тех сутках загорался бы перерасход, которого не было. На локальную оценку это не влияет: там отсечка работает по-прежнему |
| переключение аккаунта проигрывалось анимацией: полосы уезжали в ноль и приползали на новые места, будто это те же цифры изменились | строки привязаны к аккаунту (`.id(activeAccount)` в `PopoverView`) и при смене рисуются сразу на своих местах. У другого аккаунта другая неделя, а не изменившаяся своя; обычное обновление данных анимируется как прежде |
